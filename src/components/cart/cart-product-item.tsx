import { CartListItem } from "@/types/cart-list-item"

type Props = {
    item: CartListItem;
}

export const CartProductItem = ({ item }: Props) => {
    return (
        <div>
            teste
        </div>
    )
}